# Ray-Ban Meta E-Commerce Store

Современный интернет-магазин очков с админ-панелью, построенный на Nuxt 3 и FastAPI.

## 🚀 Возможности

### Для пользователей:
- 🛍️ Просмотр каталога товаров с фильтрацией по брендам
- 🔍 Детальная страница товара с отзывами и рейтингом
- ❤️ Избранное
- 🛒 Корзина покупок
- 📦 История заказов
- 👤 Личный кабинет
- 🔐 Регистрация и авторизация

### Для администраторов:
- 📊 Дашборд с статистикой (выручка, заказы, пользователи, товары)
- 📦 Управление товарами (добавление, удаление)
- 🏷️ Управление брендами (добавление, удаление)
- 📁 Управление категориями (добавление, удаление)
- 👥 Просмотр пользователей
- 📋 Управление заказами (изменение статуса)

## 🛠️ Технологии

### Frontend:
- **Nuxt 3** - Vue.js фреймворк
- **TypeScript** - типизация
- **Vanilla CSS** - стилизация
- **Google Fonts (Inter)** - типографика

### Backend:
- **FastAPI** - Python веб-фреймворк
- **SQLModel** - ORM для работы с БД
- **SQLite** - база данных
- **JWT** - аутентификация
- **Passlib** - хеширование паролей

## 📦 Установка и запуск

### Backend

```bash
cd backend

# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install fastapi uvicorn sqlmodel passlib python-jose python-multipart

# Запустить сервер
uvicorn main:app --reload --port 8000
```

Backend будет доступен по адресу: `http://localhost:8000`

### Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:3000`

## 👤 Доступы

### Админ:
- **Логин:** `admin`
- **Пароль:** `admin123`
- **URL:** `http://localhost:3000/admin/login`

### Тестовый пользователь:
Зарегистрируйтесь через форму регистрации или используйте любой номер телефона.

## 📁 Структура проекта

```
shop/
├── backend/
│   ├── main.py           # Основной файл FastAPI
│   ├── database.db       # SQLite база данных (создается автоматически)
│   └── venv/            # Виртуальное окружение Python
│
└── frontend/
    ├── pages/           # Страницы приложения
    │   ├── index.vue    # Главная страница
    │   ├── login.vue    # Страница входа
    │   ├── register.vue # Страница регистрации
    │   ├── cart.vue     # Корзина
    │   ├── orders.vue   # История заказов
    │   ├── profile.vue  # Профиль пользователя
    │   ├── favorites.vue # Избранное
    │   ├── products/
    │   │   └── [id].vue # Детальная страница товара
    │   └── admin/       # Админ-панель
    │       ├── index.vue
    │       ├── products/
    │       ├── brands/
    │       ├── categories/
    │       ├── users.vue
    │       └── orders.vue
    │
    ├── components/      # Компоненты
    │   ├── AppHeader.vue
    │   └── ProductCard.vue
    │
    ├── composables/     # Composables
    │   ├── useAuth.ts   # Аутентификация
    │   └── useCart.ts   # Корзина
    │
    ├── layouts/         # Layouts
    │   ├── default.vue
    │   └── admin.vue
    │
    └── app.vue          # Корневой компонент
```

## 🔌 API Endpoints

### Публичные:
- `POST /register` - Регистрация
- `POST /token` - Авторизация
- `GET /products` - Список товаров
- `GET /products/{id}` - Детали товара
- `GET /brands` - Список брендов
- `GET /categories` - Список категорий

### Защищенные (требуют токен):
- `GET /users/me` - Текущий пользователь
- `POST /orders` - Создать заказ
- `GET /orders/me` - Мои заказы
- `POST /products/{id}/favorite` - Переключить избранное

### Админ (требуют admin роль):
- `GET /admin/stats` - Статистика
- `GET /admin/users` - Все пользователи
- `GET /admin/orders` - Все заказы
- `PUT /admin/orders/{id}` - Обновить статус заказа
- `POST /products` - Создать товар
- `DELETE /products/{id}` - Удалить товар
- `POST /brands` - Создать бренд
- `DELETE /brands/{id}` - Удалить бренд
- `POST /categories` - Создать категорию
- `DELETE /categories/{id}` - Удалить категорию

## 🎨 Дизайн

Проект использует современный минималистичный дизайн с акцентом на:
- Чистые линии и пространство
- Премиум типографика (Inter)
- Плавные анимации и переходы
- Адаптивный дизайн (mobile-first)
- Темные акценты на светлом фоне

## 📝 Примечания

- База данных создается автоматически при первом запуске backend
- Начальные данные (товары, бренды, категории, админ) добавляются автоматически
- Пароли хешируются с использованием pbkdf2_sha256
- JWT токены хранятся в cookies на frontend
- Корзина хранится в состоянии приложения (useState)

## 🔒 Безопасность

- Все пароли хешируются
- JWT токены для аутентификации
- Защита админских эндпоинтов
- CORS настроен для локальной разработки

## 🚧 Возможные улучшения

- [ ] Добавить поиск товаров
- [ ] Интеграция платежной системы
- [ ] Email уведомления
- [ ] Отзывы пользователей
- [ ] Фильтры по цене и категориям
- [ ] Сортировка товаров
- [ ] Пагинация
- [ ] Загрузка изображений
- [ ] Экспорт отчетов для админа

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано с ❤️ для демонстрации современного e-commerce решения.
