# 🔧 Исправлена ошибка отправки рассылок

## ❌ Проблема

В логах была ошибка:
```
Bad Request: inline keyboard button URL 'SAYTDAN ZAKAZ BERING' is invalid: 
Disallowed character in URL host
```

**Причина:** В поле "Button URL" был указан обычный текст вместо URL. Telegram требует, чтобы URL кнопки был валидным веб-адресом, начинающимся с `http://` или `https://`.

---

## ✅ Решение

### 1. **Бэкенд - валидация и фильтрация**

**Файл:** `backend/app/features/broadcasts/tasks.py`

Теперь перед отправкой проверяется `button_url`:
- Если URL **не** начинается с `http://` или `https://`, кнопка **не добавляется** к сообщению
- Логируется предупреждение: `⚠️ Invalid button_url 'XXX' - skipping button`
- Сообщение отправляется **без кнопки**, но успешно

**Результат:** Рассылка больше не будет падать из-за невалидного URL.

---

### 2. **Фронтенд - валидация при создании**

**Файл:** `frontend/app/pages/shop/[slug]/admin/broadcasts/index.vue`

Добавлена валидация в форме создания рассылки:

1. ✅ **Проверка формата URL:**
   - URL должен начинаться с `http://` или `https://`
   - Иначе выводится ошибка: "Button URL must start with http:// or https://"

2. ✅ **Проверка наличия текста кнопки:**
   - Если указан URL, обязательно нужен текст кнопки
   - Иначе ошибка: "Button text is required when URL is provided"

3. ✅ **Визуальная подсказка:**
   - Добавлена подсказка под полем: "URL должен начинаться с http:// или https://"
   - Изменен placeholder на реальный пример: `https://storely.uz/s/sarvar`

---

## 📝 Как правильно использовать

### ✅ Правильно:

**Button Text:** "Заказать на сайте"  
**Button URL:** `https://storely.uz/s/sarvar`

**Button Text:** "Наш Instagram"  
**Button URL:** `https://instagram.com/yourbrand`

**Button Text:** "Whatsapp"  
**Button URL:** `https://wa.me/998901234567`

---

### ❌ Неправильно:

**Button URL:** `SAYTDAN ZAKAZ BERING` ❌ (это текст, не URL)  
**Button URL:** `storely.uz` ❌ (нет http://)  
**Button URL:** `www.storely.uz` ❌ (нет http://)

---

## 🧪 Тестирование

1. **Создайте новую рассылку**
2. Попробуйте указать невалидный URL (например "SAYTDAN ZAKAZ"):
   - Должна появиться ошибка при сохранении
3. Укажите правильный URL (`https://storely.uz/s/sarvar`):
   - Рассылка должна создаться успешно
4. Отправьте рассылку:
   - Все подписчики должны получить сообщение с кнопкой

---

## 🎯 Что теперь будет

- ✅ **Старые рассылки с невалидными URL:** Сообщения отправятся без кнопки
- ✅ **Новые рассылки:** Невозможно создать с невалидным URL
- ✅ **Пользователи:** Будут получать рассылки корректно

---

## 📌 Важно

Если вы хотите рассылку **без кнопки** - просто оставьте поля "Button Text" и "Button URL" пустыми!
